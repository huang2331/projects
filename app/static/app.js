const form=document.getElementById('upload-form'), input=document.getElementById('audio'), statusEl=document.getElementById('status'), nameEl=document.getElementById('filename');
input.addEventListener('change',()=>nameEl.textContent=input.files[0]?.name||'');
form.addEventListener('submit',async(event)=>{event.preventDefault();if(!input.files.length)return;const button=form.querySelector('button');button.disabled=true;statusEl.className='working';statusEl.textContent='Uploading and transcribing… The first local model run may take several minutes.';try{const data=new FormData();data.append('audio',input.files[0]);const response=await fetch('/api/lessons',{method:'POST',body:data});const body=await response.json();if(!response.ok)throw new Error(body.detail||'Processing failed.');statusEl.textContent='Transcript and speech analysis complete. Opening results…';location.href='/lessons/'+body.id}catch(error){statusEl.className='error';statusEl.textContent=error.message;button.disabled=false;}});
